# Playlist Editor

### Description:
    Allow the user to further customize their playlist

### Entry:
   entry.py
      add_songs_by_artist: Add songs by an artist already in the playlist (not required, but the function assumes the artist is valid)
      get_valid_track_recommendations: Return a list of SpotifyTrack s , recommended by spotify for the playlist

TODO - update length db if artists added (no) - If we are going to use the get recommendations thing to extend the playlist we will need to yupdate length of generated and the number of followers average in db

    
### Features to Implement:
   - Button to try different niche level if the request doesnt generate enough songs (delete the playlist) or just keep it (frontend)

